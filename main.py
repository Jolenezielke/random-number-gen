'''
import random 

generate a random integer and store it in a variable. myNum = random.randint(10,50) input - Ask the user to enter a number.  if/else statements. If the number the user entered equals the random number, print, you win.  if the number is too low, print "Too Low"  if the number is too high, print "Too High"
'''